# Changelog

## 0.0.1

- Initial release
- Based on `ghcr.io/bluesky-social/pds:v0.4.5006`
- Auto-generates `PDS_JWT_SECRET` and `PDS_PLC_ROTATION_KEY_K256_PRIVATE_KEY_HEX` on first run and persists them to `/data/secrets.env`
